# 离线高德瓦片

将 GCJ-02 高德街道瓦片按以下固定结构放在本目录：

```text
data/map/amap/{z}/{x}/{y}.png
```

- 支持缩放级别 `15` 至 `19`。
- `z` 是缩放级别，`x` 和 `y` 是 Web Mercator 瓦片坐标。
- 第一阶段离线模式只支持街道图，不支持卫星图。
- 瓦片缺失时应用显示灰色占位内容，并继续允许平移和缩放。
- 不要把瓦片放到源码或其他绝对路径；应用只读取可执行文件旁的 `data/map/amap`。

离线瓦片通常体积较大且可能受供应商许可限制，因此仓库只保留本说明，不提交实际瓦片文件。
